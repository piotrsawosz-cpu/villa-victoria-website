# macOS
.DS_Store
.AppleDouble
.LSOverride

# Environment variables
.env
.env.*

# Node.js (for when code scaffold is added)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build output
dist/
build/
.next/
out/

# Editor
.vscode/settings.json
.idea/

# Logs
*.log

# Uploads & large raw asset folders (not needed in repo)
uploads/
Villa Victoria Photos/
Ai Images - Villa Victoria/
Villa Victoria - Main Website-handoff.zip

# Claude Code config — personal config and memory, not project code
.claude/

# macOS Finder duplicate files (created when you duplicate a file in Finder)
*\ 2.*
*\ 2/

# Source image dumps — web-ready versions already live in media/activities/
elements activites roses/

# Legacy duplicate website folder — active site is the root-level files
Villa Victoria Main Website/

# Personal Claude overrides — not committed
CLAUDE.local.md

# Workspace-only zones (kept locally but not pushed)
archive/
inbox/*
!inbox/README.md

# Windows
Thumbs.db
